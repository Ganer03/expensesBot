# 💸 Expenses Telegram Bot

Телеграм-бот для учёта личных и семейных расходов с визуализацией статистики.

Бот позволяет:

* учитывать траты по категориям;
* ограничивать доступ только выбранным пользователям;
* автоматически строить графики расходов;
* отправлять отчёты в общий Telegram-чат;
* формировать недельные и месячные отчёты по расписанию.

---

## 🚀 Возможности

### 📥 Учёт расходов

* Пользователь выбирает категорию
* Вводит сумму
* Подтверждает ввод
* Запись сохраняется в SQLite
* Сообщение о расходе отправляется в общий чат

### 📊 Графики

* Построение графиков по:

  * выбранной категории
  * всем категориям
* Периоды:

  * текущий месяц
  * прошлый месяц
  * произвольный диапазон дат

Отправляются **два изображения**:

1. Сводка (по пользователям + по категориям)
2. Линейный график расходов по дням (каждый пользователь + общий)

### ⏰ Автоматические отчёты

* 📅 **1 числа каждого месяца** — отчёт за прошлый месяц
* 📆 **Каждый понедельник** — отчёт за прошлую неделю

---

## 🧱 Используемые технологии

* **Python 3.11**
* **aiogram 2.x** — Telegram Bot API
* **SQLite** — база данных
* **matplotlib** — построение графиков
* **pandas** — агрегация данных
* **APScheduler** — планировщик задач
* **PM2** — запуск и мониторинг бота

---

## 📦 Установка зависимостей

Рекомендуется использовать виртуальное окружение:

```bash
python3.11 -m venv venv311
source venv311/bin/activate
pip install -r requirements.txt
```

### requirements.txt (возможно еще что-то, не проверял с requirements)

```txt
aiogram==2.*
pandas
matplotlib
apscheduler
python-dotenv
```

---

## 🔐 Переменные окружения (.env)

Создай файл `.env` в корне проекта:

```env
API_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
GROUP_ID=-100XXXXXXXXXX
ALLOWED_USERS=123456789,987654321
TZ=Asia/Dubai
```

### Где взять значения

* **API_TOKEN** — создать бота через @BotFather
* **GROUP_ID** — добавить бота в группу и узнать ID (через любого ID бота)
* **ALLOWED_USERS** — Telegram user_id пользователей через любого ID бота

---

## 🗄 База данных

Используется SQLite (`expenses.db`).

Таблица создаётся автоматически при старте:

```sql
CREATE TABLE expenses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    username TEXT,
    category TEXT,
    amount REAL,
    created_at TEXT
);
```

---

## ▶️ Запуск

### Обычный запуск

```bash
python expenses_bot.py
```

### Запуск через PM2 (рекомендуется)

```bash
pm2 start /root/project/venv311/bin/python \
  --name expenses-bot \
  -- /root/project/expenses_bot.py
```

Логи:

```bash
pm2 logs expenses-bot
```

---

## 🧠 Архитектура

* FSM для ввода расходов и графиков
* Планировщик APScheduler встроен в event loop aiogram
* Генерация изображений во временные файлы с автоудалением
* Безопасный доступ только для разрешённых пользователей

---

## 📌 Команды и кнопки

* `/start` — запуск бота
* `/cancel` — отмена текущего действия
* Категории расходов — кнопки
* 📊 **Графики** — интерактивное меню

---

## 🛠 Идеи для развития

* Экспорт в CSV / Excel
* Хранение данных в PostgreSQL
* Inline-календарь выбора дат
* Web-дашборд
* Уведомления о лимитах

---

## 👨‍💻 Автор

Проект написан для личного использования и обучения.

Если хочешь — можно легко адаптировать под:

* семью
* команду
* личный бюджет

---

✨ Happy tracking! 💸
